# Copy to `.env` and fill in. Next.js and prisma.config.ts both read `.env`.

# --- Database (Neon) ------------------------------------------------------
# `neon env pull` (Neon CLI) writes both of these for you — prefer that over
# copying them by hand, and it keeps the names below in sync.
#
# Pooled connection — the host contains "-pooler". Used by the app at runtime.
DATABASE_URL="postgresql://USER:PASSWORD@ep-xxx-pooler.REGION.aws.neon.tech/neondb?sslmode=require"
# Direct (unpooled) connection — same URL without "-pooler". Used by migrations,
# because pgBouncer can't run DDL inside a transaction.
DATABASE_URL_UNPOOLED="postgresql://USER:PASSWORD@ep-xxx.REGION.aws.neon.tech/neondb?sslmode=require"
# Optional alias for the line above, for tooling that expects Prisma's name.
# DIRECT_URL=""

# --- Better Auth ----------------------------------------------------------
# Generate with: openssl rand -base64 32
BETTER_AUTH_SECRET=""
BETTER_AUTH_URL="http://localhost:3000"
NEXT_PUBLIC_APP_URL="http://localhost:3000"
# Optional: extra origins allowed to drive auth, comma-separated.
# BETTER_AUTH_TRUSTED_ORIGINS="https://staging.docsy.app"

# --- Email (Resend) -------------------------------------------------------
# Without a key, verification and reset links are logged to the terminal.
RESEND_API_KEY=""
# Must be an address on a domain verified at https://resend.com/domains.
# Resend's sandbox sender (onboarding@resend.dev) only delivers to your own
# Resend account address, so it can't be used to reach real users.
EMAIL_FROM="Docsy <hello@alimurtaza.online>"
# Optional. Transactional mail gets replied to — point this at an inbox someone
# reads rather than leaving replies to bounce off the From address.
EMAIL_REPLY_TO="support@alimurtaza.online"

# --- Social sign-in (optional) --------------------------------------------
# Each provider appears in the auth dialogs only when both vars are set.
# Google:  https://console.cloud.google.com/apis/credentials
#   Redirect URI: http://localhost:3000/api/auth/callback/google
GOOGLE_CLIENT_ID=""
GOOGLE_CLIENT_SECRET=""
# GitHub:  https://github.com/settings/developers
#   Callback URL: http://localhost:3000/api/auth/callback/github
GITHUB_CLIENT_ID=""
GITHUB_CLIENT_SECRET=""

# --- Anthropic (Claude) ---------------------------------------------------
# Powers chat answers over your documents. Create one at
# https://console.anthropic.com/settings/keys — chat stays disabled without it.
ANTHROPIC_API_KEY=""

# --- Billing (Stripe) -----------------------------------------------------
# Billing stays off until a key and at least one price are set: the Billing tab
# says so, and every workspace sits on the Free plan.
#
# Sandbox keys: https://dashboard.stripe.com/test/apikeys (or run
# `stripe sandbox create` with the Stripe CLI). Prefer a restricted key (rk_)
# over a secret key (sk_) — it needs write access to Customers, Checkout
# Sessions, Subscriptions, Billing Portal, and read access to Invoices.
STRIPE_SECRET_KEY=""
# Signing secret for the webhook that grants plans. Locally, `stripe listen
# --forward-to localhost:3000/api/webhooks/stripe` prints one (whsec_…); in
# production it comes from the endpoint you create at
# https://dashboard.stripe.com/webhooks, pointed at /api/webhooks/stripe.
STRIPE_WEBHOOK_SECRET=""

# One Stripe Product per plan, one Price per billing period — the price ID is
# what tells the webhook which plan was paid for, so an ID that isn't listed
# here grants nothing. Create them at https://dashboard.stripe.com/test/products
# (recurring monthly / yearly), then paste the `price_…` IDs.
STRIPE_PRICE_PRO_MONTHLY=""
STRIPE_PRICE_PRO_ANNUAL=""
STRIPE_PRICE_BUSINESS_MONTHLY=""
STRIPE_PRICE_BUSINESS_ANNUAL=""
